# UI 第一阶段整理记录

当前目标是清理零散样式并保留现有外观，为后续统一设计规范准备基础。
具体变量用法见 [UI 样式变量](ui-style-variables.md)。本文件记录当前实现，
不作为字号、颜色、圆角或间距应当采用哪一档的设计规范。

此前 `feature/ui` 的大范围视觉收敛方案已经回撤。原记录中的固定视觉尺度、
组件样板 `/design.html`、设计审计命令和完成计数不适用于当前代码，已移除。
暂不恢复根目录的设计规范文档。

## 当前整理范围

- GUI、Gateway WebUI 和共享 UI 使用 Tailwind v4 的 CSS `@theme`。
- 原有字号、尺寸、颜色、阴影、断点和动效保留数值；固定样式接入共享变量。
- 标准 Tailwind 类继续使用；特殊尺寸使用 `text-14px`、`h-18px` 等具名类。
- 时间变量统一采用毫秒命名；数值相同的颜色原语合并，阴影和渐变引用这些原语。
- JS 中的斜纹背景改为具名背景类；SSH 面板、楼层导航、快捷键高亮和 Git 图颜色接入变量。
- xterm 通过 `lib/terminal/theme.ts` 读取实际颜色和尺寸；Web Animations 读取实际 easing。
  这两种 API 需要解析后的值，不能直接传入 `var(...)`。
- 继续迁移简单的单处 CSS，同时保留共享规则和第三方节点适配。

## 无消费者样式的确认方式

本轮删除前进行了三层核对：

1. 在仓库中搜索完整类名，区分生产代码、测试、文档和 CSS 定义。
2. 扫描 TS/TSX 字符串与模板片段，检查动态拼接、DOM 查询及直接写入类名的路径。
3. 核对实际渲染组件、第三方依赖及样式入口；关键帧单独检查动画名引用，
   组合选择器只移除失效分支，保留仍在使用的分支。

确认删除了 58 个无消费者类名、118 个选择器分支和 10 个无引用关键帧。
主要涉及以下已失效的实现：

| 旧样式 | 当前消费者核对 |
| --- | --- |
| `checkpoint-status-pulse`、`checkpoint-shimmer` | 检查点卡片使用当前共享组件和展开动效；同前缀动态字符串是消息 ID，不是类名。 |
| `hub-chip-enter`、`hub-status-dot` 等 | Hub 页面没有对应类名或动态生成路径；仍使用的 Hub 入场动画保留。 |
| `skills-drawer-*` | 预览使用当前 Drawer/Dialog 实现，不再生成旧浮层类名。 |
| `composer-dropdown*`、`composer-reasoning-trigger` | 当前输入控件使用各自的 Base UI 弹层和状态属性。 |
| 旧 user/assistant/tool/thinking 气泡类、工具展示、页脚和退出按钮 | 核对当前 transcript 分支及用户菜单，旧节点已不存在。 |
| 旧设置工具列表、工具详情浮层及零散行样式 | 核对当前设置页面；没有生成旧类名的入口。 |
| `scroll-area-scrollbar`、`scroll-area-thumb` | 当前共享 ScrollArea 使用工具类及 `data-scroll-area-scrollbar`；属性选择器保留。 |

以下静态搜索无命中的样式有实际消费者，明确保留：

- `settings-content-*`、`settings-section-shell-*`：由设置页 ID 拼接。
- `status-board-tone-*`、`status-board-fact--*`：由状态数据拼接。
- `rct-*`：由 `react-complex-tree` 渲染。
- `xterm-*`、终端滚动条节点：由 xterm 渲染。
- Markdown 任务列表、KaTeX 和 Diff 相关类名：由对应渲染器生成。

## 单处样式与验证边界

此前已迁移 166 个单处类，本轮再迁移 18 个，涉及移动端设置行、滚动容器、
SSH 标签页、通知退出动效等。移动端共享 CSS 的优先级通过前后计算样式对比确认。

几何计算、虚拟列表测量、与运行时回调共用的时序常量、媒体查询条件和品牌 SVG
属于各自的运行契约，未通过近似换算或机械替换改动。

验证结果：

- `pnpm --filter @liveagent/ui typecheck`、`pnpm check:ui-boundaries` 通过。
- `pnpm --filter liveagent build`、`pnpm --filter @liveagent/gateway-webui build` 通过。
- `pnpm test:gui`：3,045 项通过；`pnpm test:webui`：714 项通过。
- 浏览器 24 组常规对比及 12 组减少动态效果对比，共 5,310 组元素场景；
  包含父子元素的计算样式，无差异。覆盖两端、亮暗主题、关键宽度和字号缩放。
- 两端亮暗主题的 xterm 初始化通过，每套 26 个颜色值与原调色板一致；
  Web Animations 接受解析后的曲线值。实际通知组件的自动关闭回调与退出类切换通过。
- 残留扫描未发现已删除类名的生产引用、缺失样式变量或重复时长值。
- 本轮 CSS 净减少 580 行；源码 Biome 检查通过，互斥媒体条件的优先级提示已注明原因。
  通知关闭的最终修正另跑相关测试及浏览器组件验证通过。

浏览器检查使用组件和样式夹具，不代表已经遍历全部原生终端连接、编辑器、
拖拽及真实业务流程。源码改动可通过本轮差异恢复，没有修改存储格式或运行配置。


## 按钮与输入框的代码收敛

本次继续保持原有样式，只整理重复代码。盘点共享 UI、GUI 和 Gateway WebUI 的
840 处直接 JSX 声明：310 处 Button、356 处原生 button、117 处 Input、26 处原生
input、23 处 Textarea、3 处原生 textarea、5 处 NumberInput。这是源码声明计数，
不代表运行时实例数，也不包含其他自定义组件内部生成的控件。

| 已整理项 | 消费者与改动 | 保留的契约 |
| --- | --- | --- |
| 图标按钮尺寸 | 23 处 `h-7 w-7` 改用 `size="icon-xs"`，24 处 `h-8 w-8` 改用 `size="icon-sm"`；另外 5 处移除与原 `icon` 相同的 `h-9 w-9` 覆盖。 | 两个新尺寸直接复用原 Tailwind 类；颜色、圆角、内边距、焦点、禁用态及事件处理均保留。 |
| 文本输入基础样式 | Input 和 Textarea 的 16 个相同工具类由 `components/ui/text-field-styles.ts` 维护。 | 单行高度、各自纵向内边距、textarea 最小高度、文件输入样式和 Input 的过渡效果仍由原组件维护；调用方覆盖顺序不变。 |
| 侧栏重复样式 | 项目图标按钮由 `ChatHistorySidebar` 与 `ChatHistorySidebarRows` 共用 `ChatHistorySidebarStyles.ts`；当前组重命名输入框由 Rows 中的 `ProjectGroupHeader` 消费该模块。 | 共享字符串保留原值；菜单打开态、透明背景、缩放字号和键盘处理不变。2026-09-10 合入 main 后，原侧栏内创建组输入框已随上游流程调整移除。 |

这些调整消除了重复维护的样式组合，没有新增包装组件、依赖或全局 CSS，也未改动
默认 Button 尺寸及普通文本按钮。现有样式变量仍是具体数值的维护入口。

以下候选经核对继续保留，避免改变现有行为：

- 原生图标按钮和 Button 的 hover、焦点、禁用透明度并不一致。例如图片预览的
  禁用透明度为 35%，基础 Button 为 50%；整类替换会改变外观。
- Remote/CUA 的局部复制按钮与通用 CopyButton 在尺寸、提示和复制反馈时序上
  有差异，本次未合并复制逻辑。
- NumberInput 的 Base UI 组容器、增减按钮及无效状态不等同于文本输入控件。
- 原生 input 中包含文件、单选、内嵌搜索等用途；未机械替换成带边框的 Input。
- `ProjectToolsPanelToggle` 在调用方 className 之后强制指定尺寸；直接把尺寸移至
  Button 默认变体会改变覆盖优先级，因此保留原写法。
- 侧栏图标样式还供 DropdownMenuTrigger 直接使用；尺寸继续留在这份共享样式中。

本次验证：

- 改动前后共享 UI 类型检查均通过；UI 边界检查和改动源码 Biome 检查通过。
- GUI、Gateway WebUI 生产构建通过；前端测试分别为 3,045 项、714 项，全部通过。
- 2,205 组修改前后真实 React 渲染比较无差异，包含六种按钮外观和调用方覆盖；
  比较时仅忽略 class 属性内部的类名顺序，保留类名集合、标签及其他 DOM 属性。
- 浏览器覆盖两端、亮暗主题、1600/380 像素宽度，共 8 组；每组 957 个控件场景的
  普通和禁用状态计算样式一致，另检查 Button、Input、Textarea 的实际焦点状态。
- 实际组件的默认按钮类型、链接组合、ref、输入事件及调用方样式覆盖验证通过。
  三处依赖旧尺寸写法的源码断言改为检查新尺寸属性及原交互样式。

验证使用实际基础组件与两端样式入口，不代表逐页遍历了全部业务流程；hover 等
伪类的工具类集合保持一致，但本轮未逐个进行鼠标悬停截图对比。恢复时仅反向应用
本次组件、消费者、测试和本节文档的差异，保留此前已经暂存的变量与 CSS 整理。

## 2026-09-10 合入 main

本次在 `feature/ui-v2` 的 `9775057d` 上整合本地 `main` 的 `529ae77c`，共同基线为
`5c1b0e78`。获取远端 main 时连接超时；本地 main 与已有的 origin/main 引用一致，
但这不能证明远端在检查时没有更新。以上各节的验证数字属于此前整理批次。

处理了侧栏主体、侧栏行、HubChrome、MCP Hub 和 Skills Hub 五个文件的文本冲突，
同时检查了自动合并及新增 UI 文件：

- 保留 main 的工作区会话树、独立置顶排序、拖拽插入标记、归档处理及资源快捷入口。
- 保留资源页内嵌模式和创建目录/克隆流程调整；沿用当前 Tailwind v4 变量入口及控件尺寸。
- 将新增的固定字号、宽度、拖拽浮层移出视口的默认值和加载动画时长接入已有变量。
  资源管理页的 1320px、快捷入口设置的 920px 宽度同样使用已有具名类，没有新增等值原语。
- 已删除 CSS 的原消费者继续使用等效工具类；未恢复根目录设计规范或 Tailwind JS 配置。
- 补齐侧栏测试桩中缺失的 `Brain`、`Clock3`、`MessageSquare` 图标。原有六项失败已在
  未修改的 main 侧栏源码上复现；修复仅增加图标桩，保留全部断言。

本批次验证：

- 共享 UI 类型检查、UI 边界检查、七个手工处理源码文件的 Biome 检查通过。
- GUI、Gateway WebUI 生产构建通过；GUI 前端测试 3,091 项、WebUI 前端测试 718 项全部通过。
- 对 main 新增或修改的 18 个 TSX 文件做排除样式表示后的 AST 比较，业务结构一致；
  该比较保留标签、事件、状态分支等结构，不替代对样式及控件尺寸的单独检查。
- 浏览器对比 main 与合并结果的 108 组样式组合，覆盖两端、亮暗主题、
  1600/821/820/380px 宽度及普通/禁用态，共 32 组、3,456 个元素场景，计算样式无差异。
  Button/Input 使用各版本真实组件合并后的类名，Web 使用实际根元素标记；比较包含子元素
  与伪元素的标准计算属性，排除作为迁移对象的 CSS 自定义属性。夹具隔离了鼠标偶然悬停，
  未据此宣称 hover、焦点、完整动画时间线或真实拖拽流程均已验收。

以上构建和测试覆盖前端；本轮没有执行 Rust 测试、真实原生文件操作或逐页业务验收。
最终检查时合并提交为 `e9c42821`，两个父提交分别为 `9775057d` 和 `529ae77c`；
提交中的源码与本批次验证结果一致。本节审计记录在该提交后补充。

## 2026-09-10 Common CSS 瘦身

基线为本轮开始时已暂存的 `feature/ui-v2` 内容（`c919afb2`）。本轮只调整样式的
代码表示与重复加载结构；不变更设计、业务状态及交互逻辑，不执行提交。

五个 `common-*.css` 文件合计从 1,858 行降至 1,442 行，减少 416 行（约 22%）。
简单进入动画、设置隐藏态、SSH 认证面板过渡、侧栏文字遮罩及进度装饰改为标签上的
Tailwind 类，表达式复用 `tokens.css` 的 Tailwind v4 变量。CSS 行数减少并非全部
代码删除：动画、渐变和阴影的值迁入集中入口，关键帧仍保留在 common 文件。

多处复用的加载样式抽成 `Skeleton`、`LoadingSurface` 变体，以及 `LoadingTrack`
和 `FrostSpinner`。Skills 与 MCP 的两份相同旋转指示器实现合并。调用方保留原标签
所承载的内容、尺寸和属性；组件透传 ref 与事件。仅共享动画而标签、语义和交互不同
的节点使用具名工具类，不为减少类名强加通用业务组件。

本轮核对后保留：

- Markdown、Streamdown、KaTeX 与门户内容选择器：消费节点由第三方生成，且受字号区域影响。
- Git diff 调整宽度占位的伪元素、命令式滚动条：涉及复杂绘制及脚本创建的节点。
- Sidebar/Base UI 菜单状态与子项动画、Hub 父子层叠和卡片/扫描点分段延迟。
- Provider 工具栏：两端消费与响应式覆盖不同，不能仅凭共享类名归并为相同布局组件。

验证：

- GUI 3,093 项、WebUI 718 项测试通过；共享 UI 类型检查、UI 边界及改动源码 Biome 检查通过。
- GUI 与 WebUI 生产构建通过（GUI 仍有大 chunk 提示）。
- 150 组 DOM 场景排除 class 属性后结构一致，包括真实渲染的完整 Skills 加载区域。
  浏览器覆盖两端、亮暗主题、1600/380px 宽度与普通/减少动态效果分支，共 16 组。
  对动画 0ms、500ms 的元素、子元素、伪元素标准计算属性进行比较，未发现差异。
- 比较不包含作为迁移对象的 CSS 自定义属性；只归一化 Tailwind 为阴影组合插入的
  全透明、零尺寸阴影层。颜色差异未忽略，已改为原 HSL alpha 表达式。
- 覆盖设置隐藏标记、通知与右键菜单退出态、六个脉冲延迟位置；组件 DOM 测试验证
  ref、属性、子元素及点击事件透传，另验证 `cn` 的渐变/背景色与阴影覆盖。

减少动态效果的浏览器验证通过夹具切换 CSS 媒体条件，未改变系统偏好或测试
`matchMedia`；样式夹具不代表逐页业务流程、原生操作、hover/焦点或完整动画时间线
均已验收。恢复时仅反向应用本节涉及的消费者、加载组件、token、CSS 与测试文档差异，
保留本轮开始前已经暂存的改造。

## 2026-09-10 等值宽高合并

在本轮开始时的工作区基础上，扫描 crates 下的前端源码与 CSS/HTML：179 个源码文件
中的 1,704 组等值宽高，以及一处 CSS `@apply`，统一为 `size-*`。只合并同一个类名
表达式内、值和变体前缀相同的宽高，保留原重要性。普通态与响应式条件不同、不同
条件分支或实际宽高不同的组合继续保留。未新增尺寸 token、修改布局或交互逻辑。

验证包含 179 个源码文件展开 `size-*` 后的 AST 等效比较、8,455 组 `cn` 合并比较
（含调用方独立宽度、高度及整体尺寸覆盖），以及双端实际 Tailwind 入口编译的
45 种尺寸类宽高声明对比，均通过。共享 UI 类型检查、改动源码 Biome、UI 边界与
双端生产构建通过。GUI 的六处旧类名源码断言更新为新写法，保留原尺寸与行为约束。
GUI 3,093 项、WebUI 718 项前端测试全部通过。
本轮验证针对语法、生成的 CSS 和类名合并，不代表新增了逐页浏览器视觉验收。

## 2026-09-10 动画定义集中管理

参照 crisp3d 的集中定义方式，将 11 个 CSS 文件中的 70 组关键帧，以及原先
`tokens.css` 中的 58 个动画变量，迁至共享 `styles/animations.css`。`base.css`
统一导入，两端和组件沿用现有类名。时长、曲线等基础值继续复用原 token；关键帧
原有层级与所有属性值保留。空的 `common-chat-hero.css` 及其唯一导入被移除。

状态选择器和减少动态效果规则仍是各消费者的一部分，没有集中套用新的默认效果。
同步更新输入框动画注释、通知测试引用与变量使用说明。当前动画定义入口以本节和
`ui-style-variables.md` 为准，此前批次关于关键帧留在 common 文件的记录属于历史状态。

验证逐项比较了迁移前工作区的 70 组关键帧和 58 个变量，内容一致；双端实际样式入口
编译后的非关键帧声明一致（仅归一化格式空白和互不覆盖的主题变量声明顺序）。
双端生产构建、改动源码 Biome 检查通过；GUI 3,093 项、WebUI 718 项前端测试通过。
GUI 仍有既有大 chunk 提示。本轮是定义位置调整，没有新增逐页
浏览器动画验收，也未提交。

## 2026-09-10 复用规则整理

本轮基于开始时的工作区保存基线，完成七类整理和一次性验证，保留此前暂存内容。

- 四处筛选 TabsList 和三处计数 Badge 改为组件变体；八处空态、六处步骤编号、五处
  选项卡片提取展示组件。不同 Trigger 间距、状态颜色、选择逻辑和按钮行为继续保留。
- 34 个文件中的 62 组 padding、gap、定位等值组合改为简写。同前缀、同重要性且没有
  重叠轴覆盖才合并；在现有横排书写模式下，对实际编译属性逐项比较一致。
- 确认弹层与 Tooltip 的重复状态规则合并，距离及进出时长保留原值；退出关键帧同步
  接入新的公共位移变量。保留状态选择器的特异性和减少动态效果优先级。
- `cn()` 登记三类复合 token 名称；把跨组件使用的迁移期
  `shadow-ui-mentioncomposer-8` 改为 `shadow-editor-context-menu`，数值不变。
- 六处曾绑定 size/宽高字符串的测试文件改为结构化尺寸检查，保留位置、图标和交互断言。
  新增真实 DOM 组件测试以及全部登记 token 的编译和覆盖检查。
- 迁移期间用临时检查和真实 React 组件矩阵核对结果；这些一次性工具验收后不进入仓库。

本轮浏览器基线使用修改前源码和 CSS；新抽出的展示组件在基线夹具中用原消费者的
字面样式包裹原生元素，Tabs/Badge 使用原调用方 className。适配仅在临时验证目录，
不进入产品。矩阵不等于逐页业务流程验收；减少动态效果仅模拟 CSS 分支。

验证结果：

- 48 个消费者文件排除明确的展示替换后，业务 AST 一致。
- 37 个样例 × 32 组环境，比较默认／焦点状态及动画 0ms／500ms 的元素和伪元素，
  标准计算样式无差异；包含确认弹层顶部／底部退出关键帧。
- GUI 3,096 项、WebUI 718 项、检查脚本 16 项测试通过。共享 UI 类型检查、UI 边界、
  增量防漂移、改动源码 Biome 与双端生产构建通过；GUI 仍有既有大 chunk 提示。
- CI 的 Shared UI Boundaries job 使用实际 base SHA 执行增量检查，YAML 本地解析通过；
  本轮未触发远端 CI。根工作区补充工具直接使用的 PostCSS 开发依赖，复用锁文件中已有
  的 8.5.8，不升级依赖版本。登记表按数据内容校验，格式化不会造成误报。

本轮未提交。恢复应反向应用本节涉及的组件、消费者、样式、测试、脚本、CI 和文档差异；
不要回撤此前各轮已经暂存的整理。预览服务只用于本地验证，结果不替代完整业务验收。

## Gateway 独立样式目录瘦身

在不调整数值、单位、交互和主题的前提下，将编辑器宿主、转录滚动容器、分享页元信息、
登录特性列表及文字、同步加载点容器、状态面板的简单布局迁移到标签工具类。
登录文字的 820/640px 和列表的 640/380px 响应式规则随消费者迁移。

六处状态面板容器复用 `StatusPanel`，仍渲染原生 `section`，共享基础布局和原有
宽度/高度 OR 条件下的内边距；事件流面板保留默认底部 12px、紧凑模式 10px。
复杂装饰、跨节点状态、第三方控件、滚动与安全区规则继续留在 CSS。
没有新增 token，也没有将 px 换成 rem。目录 CSS 从 2,643 行减至 2,495 行。

验证：Web 718 项测试、Web 生产构建（含 TypeScript）、改动源码 Biome、UI 边界及
样式漂移检查通过。六个消费者文件排除 className、格式空白和明确的 section 组件替换后
业务 AST 一致。使用修改前保存的 CSS 与当前真实 Tailwind 编译结果，对 17 个样例进行
20 组亮暗主题、断点边界/两侧及高度条件比较，元素和伪元素的标准计算属性无差异。
此夹具验证迁移的布局与响应式声明，不等同于已完成登录后的完整业务流程验收。

### Gateway 样式目录第二批

基于上一批结束后的工作区重新保存基线，将登录页 Logo、特性卡片和特性图标的布局、
820/640/380px 响应式尺寸迁入原消费标签；保留 hover、active、触屏、主题配色和动画 CSS。
分享页加载、错误、空内容三种状态共用本页 `SharedHistoryStateLayout`，继续渲染同一个
原生 div，不改数据请求、条件分支或子内容。未新增样式 token，未调整原数值或单位。
本批再减少 78 行 CSS，目录合计 2,417 行。

Web 718 项测试、生产构建、改动文件 Biome、UI 边界与增量样式检查通过。
两个消费者归一化展示类名和明确的容器替换后，业务 AST 一致。
5 个布局样例在 20 组亮暗主题及断点边界环境中，元素和伪元素计算样式无差异。
夹具同步迁移父容器样式，并在主题颜色过渡完成后采样；不将尚未稳定的过渡帧当作布局差异。
此次未执行登录后完整业务流程验收。

### Gateway 整目录批量整理

本批以此前两轮完成后的工作区（2,417 行）为基线，覆盖四个文件及其消费者。
目录降至 1,936 行，本批减少 481 行；从这轮目录整理最初的 2,643 行累计减少 707 行。
没有新增数值 token、调整单位或更改交互逻辑。

- 批量迁移状态面板排版、布局、间距和尺寸，以及登录、同步加载、聊天容器的可独立布局。
  等值宽高同步合并成 `size-*`；颜色直接引用既有变量。
- 19 处重复标题、说明标签和区块标题容器收进 `StatusTypography` 的三个展示组件，
  保留 p/h3/div 标签；已有 `MetricTile`、`FactList`、`StatusPanel` 继续承担各自复用责任。
- 转录区与输入框共用一份网格列定义，保留可调宽度、作用域变量及退役头像列的补偿。
- 四类隐藏原生滚动条的规则按原选择器分组合并，保留特异性和全局覆盖顺序。
- 更新三处绑定旧源码写法的测试：通过 CSS 选择器成员读取真实声明，并用 JSX AST
  验证两条渲染路径中的 ChatComposerBar 确实位于 stage 后代，不再只比较字符串位置。

全目录审计后的保留项：

| 文件 | 保留原因 |
| --- | --- |
| `base-chat.css` | Gateway 全局字体/焦点覆盖、第三方文件树与滚动条、虚拟视口生成节点、动态网格和输入区定位；通用 input/button 兼容规则不按一次无命中删除。 |
| `responsive.css` | 抽屉和工具面板的状态过渡、合成层/背面可见性、共享设置与聊天组件的宿主覆盖，以及安全区与旧视口单位回退。 |
| `login.css` | 亮暗渐变、动画/触屏状态、特性配色上下文、覆盖共享 Button/Textarea 的原生控件规则，以及终端生成节点的触控行为。 |
| `status-board.css` | 渐变/遮罩/伪元素绘制、动态 tone 变量、共享表面装饰、动画和雷达的视口尺寸表达式。 |

层叠核对中保留了原有两个容易误改的行为：品牌区域子 div（包括 Logo）的原始 flex
布局，以及 380px 登录壳 padding 对安全区 padding 的覆盖。安全区整组声明保留 CSS，
没有通过近似值或更改媒体条件解决对比差异。

验证：Web 718 项测试、Web 生产构建（含 TypeScript）、改动源码 Biome、样式漂移和
UI 边界检查通过。9 个修改消费者在归一化类名、JSX 格式空白及明确的原生标签组件替换后，
业务 AST 一致。136 个迁移节点保留实际祖先类名，比较 24 组亮暗主题、宽度断点及
高度条件；元素与伪元素标准计算样式无差异，动画固定在同一个 500ms 采样点。
夹具不等同于登录后所有业务流程或全部动画时间点验收。未执行提交操作。

### CSS 业务规则清空

在上述阶段性整理之后，继续把 Gateway 和共享 UI 中可由 Tailwind 表达的规则迁到消费
组件，包括伪类、伪元素、属性状态、移动端断点、安全区、触屏和减少动态效果分支。
`base-chat.css`、`responsive.css`、`login.css`、`status-board.css`、`transcript.css`
以及五个 `common-*.css` 业务样式文件现已删除。

当前保留的 CSS 只承担设计 token、关键帧、自定义 Tailwind utility、字体注册、根节点
默认值、浏览器全局滚动条和 Streamdown document-level portal 等无法由 React 消费节点
携带 class 的规则。重复的 Markdown、Hub 动效和 Gateway 布局组合分别集中为 TypeScript
样式常量，重复结构则继续使用展示组件，避免把新的业务选择器放回全局样式表。

### 设置与配置表单收口（2026-09-11）

本批以拉取后的工作区为基线，覆盖 Provider、MCP 手动/注册表配置、SSH、定时任务、
Hook、系统设置、HTTP 请求编辑和 Agent 模板。76 处字段容器、78 处标签、5 处说明文字
迁入 `FormField` / `FormFieldLabel` / `FormFieldDescription`，保留默认和紧凑两档样式。
普通设置分组与 CUA 步骤的两处相同卡片表面归入 `SettingsSurface`，阴影去掉迁移编号，
改为 `shadow-settings-surface`；同步更新类名合并注册表，数值保持不变。

保留项：特殊字号/行高、带操作按钮的标签行、条件校验提示、不同卡片表面继续原样；
已有 SettingsRow、SettingsChoiceRow、DialogActions 和基础控件没有重复实现。
本批不调整请求、加载、校验、保存或键盘处理，不新增全局 token 尺度。

验证记录：

- 11 个消费者与本批修改前源码做 AST 对比，展开展示组件并归一化类名和格式后一致，
  业务条件、处理函数、控件属性和层级保留。
- 新增真实 React DOM 测试，验证两档样式、标签关联、说明 ID、ref、原生属性透传，
  以及重渲染后输入节点、未保存输入和焦点保持。
- 共享 UI 类型检查、UI 边界检查、改动 TSX Biome、两端生产构建通过。
- 设置目录 394 项测试和 Gateway 718 项测试通过。
- Gateway 当前停在认证入口，未执行登录后整页业务流程验收；浏览器夹具加载实际 React
  组件和 Gateway CSS，比较元素及伪元素样式。宿主标记切换不能替代桌面宿主的独立验收。
- 浏览器计算样式共 128 组无差异：390/1440px 宽度、亮暗主题、16/20px 根字号、
  Gateway/无 Gateway 标记、两档字段、默认/焦点与启用/禁用状态；包含伪元素。
  对比使用同一份应用 CSS，阴影改名的数值等价另由源码及 token 编译检查确认。
- 额外执行 `pnpm test:gui`：该次报告 3092 项通过、1 个测试文件进程失败
  （`transcript-width-controls-history-switch.test.mjs`）；单独以 Node TAP reporter
  重跑该文件，7 项全部通过。该次全量运行不记为全量通过，失败原因尚未确定。
- 拉取后本地依赖缺失；使用 manifest 指定的 pnpm 10.32.1 与 frozen lockfile
  完成依赖同步，未修改依赖声明或锁文件。本轮没有执行提交。

## 字号收敛到 Tailwind 档位（2026-09-11）

经用户确认，普通 UI 字号从数值 token 收敛为 Tailwind 标准 rem 档位。
删除 px/rem 数值字号、默认字号到数值 token 的别名，以及 `text-scaled-*px`；
共享 UI、GUI 与 Gateway 消费者同步迁移。小徽标和计数保留一档 `text-tiny: 0.625rem`，
Markdown 行内代码与 KaTeX 保留相对段落的 em。具体归并表见 `ui-style-variables.md`。

区域和弹层继续通过 `base.css` 覆盖标准字号变量，并覆盖新用到的 tiny、3xl–5xl。
已有显式行高保留，其他文字采用标准档位默认行高。xterm 的 API 数值字号独立保持 13px。
这次有意改变字号档位和默认行高，不承诺与旧排版逐像素一致。

验证：GUI 和 Gateway 生产构建（含 TypeScript）、Web 718 项测试、UI 边界检查通过。GUI 全量初跑有一个测试文件未完成，
单独重跑通过，随后完整重跑 3097 项全部通过；本批次改动源码 Biome 检查通过。
字号编译与类名合并测试覆盖默认行高、显式行高和 em。浏览器通过共享真实 CSS 入口的
局部样例检查了 16/20px 根字号、100%/125% 区域字号、弹层与行内代码：正文分别为
14/17.5/17.5/21.875px，行内代码保持正文的 92%；显式 leading-5 保持 20/25px。
已查看样例截图；该验证不代表完整业务页面或所有弹层已完成视觉验收。

本批次保留开始时已有的表单整理，没有执行暂存或提交操作。

Markdown 行内代码和公式的局部比例直接写在消费处：`text-[0.9em]`、
`text-[0.92em]`、`text-[1.04em]`，不注册全局字号变量。

## rem 尺寸与字重去重（2026-09-11）

移除 68 个数值命名的 rem spacing token（含负值）及 5 个数字字重 token。
组件中的常规尺寸改用标准 Tailwind 类；局部特殊尺寸保留精确 rem 值。
CSS/内联表达式中的 rem 常量直接展开，复杂语义尺寸、px 尺寸和其他视觉值保留。
450 字重改用 `font-[450]`，600 的 CSS 引用改用 `--font-weight-semibold`，
同步移除类名合并中数字字重 token 的扩展配置。

验证：双端生产构建（含 TypeScript）、GUI 3097 项测试、Web 718 项测试、
UI 边界、改动源码 Biome 检查通过；删除的 rem 变量无残留引用。
浏览器抽样对比 37 对实际使用的尺寸/字重类，在 16px、20px 根字号下共 74 组
计算样式无差异；这是工具类级验证，不代表完整业务页面的视觉验收。

### 模型与分支弹层退出跳变修复（2026-09-11）

录屏涉及模型 Popover 和 Git 分支 Dropdown。退出类使用 Tailwind v4 独立的
`scale` / `translate` 属性，原 transition-property 仅包含 transform 和 opacity，
导致缩放/位移直接跳到退出值。Popover 补入 scale，分支菜单补入 translate 和 scale。
保留原时长、曲线、退出幅度及 reduced-motion 分支，不改菜单开关和数据清理逻辑。

验证：共享 UI 类型检查、改动源码 Biome 和新增 Tailwind 编译回归测试通过。
此结论确认退出属性遗漏，不代表已经完成桌面 WebView 的性能采样或排除其他卡顿原因。

## 状态颜色第一批试点（2026-09-11）

评估和映射见 `ui-color-migration.md`。新增六个成功/错误颜色角色，成功 Badge 保持
现有配色，错误 Badge、工具错误标题/详情和审批错误提示收敛到 red 色阶。
分类色、主操作色、主题底色及原始 ui-color 定义本批保留。

双端生产构建（含 TypeScript）、GUI 3098 项测试、Web 718 项测试、UI 边界和本批
四个源码文件 Biome 检查通过。亮暗十个局部样例实测文字对比度均超过 4.5:1，
已查看前后对比图；未宣称完成所有业务背景和交互状态的视觉验收。

## 状态色纳入 shadcn 语义变量（2026-09-11）

参考 Crisp3D 的变量结构，移除上一阶段六个 status-success/error 变量，错误统一到
现有 destructive，新增 success 配对。值在 root/dark 定义，semantic-colors.css
使用 @theme inline 映射；Badge 管理浅底和边框的 alpha。其他旧 HSL 角色保持原格式。
已迁移 destructive 的内联和渐变消费者，危险操作按钮也随新颜色变化。

双端构建、GUI 3098 项、Web 718 项测试、UI 边界和改动源码 Biome 通过。
浏览器 14 个亮暗局部样例的文字对比度均超过 4.5:1，包含危险按钮默认和 hover
配色。深色危险按钮的浅红底深红字是明确的视觉变化。详细映射与边界见
ui-color-migration.md；本批没有执行暂存或提交操作。

### 设置卡片表面与状态表达（2026-09-11）

本批以当时工作区为基线，保留同时进行的颜色与字号改动。
5 张 Cron/Hook 类型选择卡片统一通过 ChoiceCard 的 kind/selected 表达选中状态；
未选中、command/http/prompt 三种选中色、原生按钮事件和 disabled 行为保持不变。
3 处折叠面板、2 处 MCP 配置分组、3 处 Provider 说明分别接入 SettingsPanel/SettingsHint；
2 处 MCP 校验提示、2 处 Provider 警告、2 处 CUA 安装提示、2 处表单内联错误接入
SettingsNotice 的四种变体。保留原 div/p 标签、子节点、图标、条件及 aria 属性。

两个仅在 SettingsSelectTrigger/Content 消费的编号阴影移回控件的 Tailwind 类，
继续引用现有变量，移除全局定义及注册项。全仓检查没有旧名称残留。
不同提示的透明度、边框、圆角和排版不近似合并；ResourceSelectionCard、状态 Badge、
特殊可拖动 Provider 卡片和 CUA 错误块保留原有专门实现。

验证：8 个页面消费者在展开展示组件、恢复状态类及等价阴影写法后 AST 一致；
真实 React DOM 测试覆盖七种面板/提示及六种选择卡片状态，验证原生标签、class 集合、
ref、属性透传、点击与禁用行为。两个阴影工具类与旧定义的 Tailwind 编译声明一致
（只归一化 HSL alpha 分隔符周围的无意义空白）。共享 UI 类型检查、UI 边界、改动
TSX Biome、双端生产构建、设置 396 项测试、Gateway 718 项测试通过。
此次未执行桌面端完整流程或全页面截图对比，未提交。

### 仪表盘颜色语义化试点（2026-09-11）

仪表盘正文、辅助文字、面板和在线状态迁移至标准语义变量；保留局部深色主题
和雷达分类色。三个 TSX 文件的原始文字颜色引用由 44 处收敛到 1 处，
删除六个局部旧别名，共享原始色定义本批未删。Web 构建与 718 项测试、
UI 边界、改动源码 Biome 通过；完成大屏静态前后对照和宿主亮暗主题检查。
模拟数据预览不代替真实事件流及响应式交互验收。详见 ui-color-migration.md。

### 无消费者颜色定义清理（2026-09-11）

删除 23 个无消费者的原始颜色定义和 1 个 Tailwind 颜色映射，原始颜色由
269 个降至 246 个。保留阴影、渐变和其他主题映射仍使用的颜色，既有工作区
改动保持原样。双端实际入口编译 CSS 除删除声明外一致，双端生产构建、
UI 边界、tokens.css Biome 通过。具体核查范围见 ui-color-migration.md。

### Cron / Hooks 状态色收敛（2026-09-11）

两页的启用统计及 Cron 错误/次数耗尽提示接入 success/destructive，
保留任务类型与生命周期分类色、布局和逻辑；未新增 token。双端构建、
396 项设置测试、Biome、UI 边界通过，查看亮暗状态局部对照。
未验证完整自动化执行流程，详细视觉变化见 ui-color-migration.md。

### 黑白透明原语迁移（2026-09-11）

57 个原始黑白透明色及 12 个映射迁移到标准 black/white 与原透明度表达式；
原始颜色由 246 减至 189。保留完整阴影/渐变、终端精确颜色及彩色原语。
新增原始颜色声明检查接入 UI 边界，剩余名称清单仅供渐进删除。
双端编译声明归一化后等价，57 对浏览器 RGBA 样例最多相差 1/255。
本批未执行暂存或提交操作，详情见 ui-color-migration.md。

### 立体快捷键键盘组件化（2026-09-11）

从 GlobalShortcutsSection 提取 ShortcutKeyboard、布局数据和局部 CSS，
移除 GHK_STYLE 字符串注入。基础键帽排版、标注定位等移至 Tailwind；
立体表面保留局部样式，键帽状态通过变量覆盖复用阴影。四组分类色由
键帽、提示点和图例共用，移除 ACTION_COLORS 与编号 CSS 色值的双重维护。
快捷键录制/注册逻辑、布局尺寸、主题颜色与状态优先级保持不变。

浏览器使用本批工作区基线提取的旧键盘和实际新组件，在隔离 iframe 中
比较 24 个场景（61/87/104 × 亮暗 × 520/1050 宽 × 录制开关），覆盖
普通、绑定、保持、按下、Enter 及提示点。4224 个节点的几何和选定计算样式
一致；圆形提示点的 50% 与 rounded-full 按圆形等效比较。
该夹具固定 macOS 键位，未验证 Windows 原生全局快捷键注册。
新增 DOM 测试覆盖布局切换、数字区跨行键、状态切换、提示和 observer 清理。
GUI 生产构建（含 TypeScript）、399 项设置测试（含 8 项快捷键测试）、
UI 边界和改动源码 Biome 通过。
本批未执行暂存或提交命令。

### 复制操作与反馈收口（2026-09-11）

覆盖通用 CopyButton、CUA/远程设置、Git 分支、文件树菜单、两个分享弹窗、本地/SSH
隧道和后台任务卡片，共 10 个原消费者文件。剪贴板能力移到 shared/clipboard，已支持
execCommand 兜底的入口统一调用；分享与后台任务保留原生 Clipboard API 及原失败处理。
通用复制控件保留旧 clipboard 函数导出，未改变外部导入契约。

成功反馈统一由 useCopyFeedback 管理；保留四档原反馈时长，支持布尔值和条目 ID。
重复复制（包括同一项）从最新成功操作重新计时，reset/卸载取消计时器，卸载后的异步
结果不会重新启动反馈计时器。CUA/远程按钮复用 SettingsCopyButton，两档原尺寸、圆角、
图标和 title 行为保留，并修复原先未等待复制完成就显示成功的问题。兜底复制失败时
保证移除临时 textarea。没有把 Tooltip、失败 toast 或菜单关闭策略统一成一种交互。

验证：10 个消费者 JSX 返回结构与本批基线一致（允许明确的设置按钮组件替换）；新增
真实 React DOM 测试验证原生写入、拒绝/缺失时兜底、失败清理、延迟成功反馈、四档计时、
快速重复制、切换目标、reset 和卸载。原 Skills 复制测试更新为追踪新的剪贴板维护入口，
同时继续验证消费者调用和受限 WebView 兜底契约。双端构建、UI 边界、改动源码 Biome
检查通过；本批未执行真实系统剪贴板权限交互或完整桌面流程验收。未提交。

本批测试结果：复制相关定向测试 8 项、Gateway 全量 718 项通过。GUI 全量复跑
3099 项通过、1 个文件级失败，失败文件为 transcript-width-controls-history-switch；
日志仅报告 test failed，未给出具体断言原因。该文件随后单独复跑 7 项全部通过，
因此尚不能将 GUI 全量标记为通过，也未据此认定具体根因。

### 设置操作错误提示复用（2026-09-11）

Cron/Hooks 操作错误提示接入 SettingsNotice 的 action-error 变体。
Hooks 的 shrink-0 保留在消费者，两个页面的错误条件、图标和截断不变。
扩展现有设置表面 DOM 测试覆盖默认和 shrink-0 规格；双端生产构建、
UI 边界、改动源码 Biome 与该 DOM 测试通过。未执行全业务浏览器验收。

### 紧凑状态徽标（2026-09-11）

Badge 新增 compact 规格并迁移 Cron 调度错误、Agents 全局启用标记；
只统一组件表现，保留状态判断、错误 title 和图标。Agents 采用主题 success，
普通及 filter-count 规格不变。亮暗局部浏览器对照确认 flex 上下文中的宽高
一致；DOM 展示原语测试通过。详见 ui-component-drift-audit.md。

### 设置页空状态复用（2026-09-11）

Cron 无任务与 SSH 导入无候选接入 EmptyState settings/compact，保留
Cron 的 block 段落排版、SSH 的 flex gap 和原显示条件。既有默认规格不变。
8 个亮暗宽窄局部浏览器场景前后对照一致；双端构建、4 项展示原语 DOM 测试、
UI 边界及改动源码 Biome 通过。未执行完整任务/导入业务流程验收。


### 分享开关接入标准 Switch（S1，2026-09-11）

HistoryShareModal 与 SharedHistoryManagerModal 的 ShareSwitch 接入共享 Switch lg。
保留原生 button、原尺寸和焦点环、禁用态、外部受控状态及管理列表只关闭的行为。
现有 default/sm 与 success tone 保留。两端构建、边界、源码 Biome、6项定向测试通过；
桌面 CSS 浏览器夹具16组几何/样式对照通过，Enter/Space 各单次回调。
未验收完整分享服务端流程与 Gateway 视觉，未运行全量测试；未提交。
详细证据与下一批范围见 ui-shadcn-migration-plan.md 第8节及 S2/S3。


### 设置开关接入标准原语（S2，2026-09-11）

SkillsSettingsForm 与 DialogSwitch 使用共享 SwitchRoot/SwitchThumb，保留原样式与
原生按钮；DialogSwitch 五处消费者保持不变，Skills 保留锁定与选中列表。
双端构建、边界、源码 Biome、3项定向测试通过。两端 CSS 的48组浏览器夹具比较及
两类控件的 Enter/Space 单次回调检查完成；一处浮点级坐标差稳定后复核一致。
未验证完整业务持久化和原生 WebView，未跑全量测试，未提交。
详见 ui-shadcn-migration-plan.md 第9节。


### 资源开关接入标准原语（S3，2026-09-11）

ResourceActivationSwitch接入SwitchRoot/SwitchThumb，保留两档原样式和卡片隔离策略。
DOM测试发现并修复隐藏input的额外click冒泡；display:contents边界仅拦截内部事件。
三处直接消费者未改。26项定向测试、GUI全量3112项、Gateway全量718项、双端构建、
边界和源码Biome通过。两端64组夹具对照通过（几何容差0.0001px），真实键盘隔离检查通过。
未执行实际技能/MCP持久化或系统WebView验收，未提交。详见迁移计划第10节。


### 会话顶部 Tabs 保守迁移（T1，2026-09-11）

ConversationViewTabs接入标准Tabs，通过render维持原单层列表；新增plain规格保留原皮肤，
不搬移内容、宿主或视图状态。方向键只移焦点、Enter/Space激活，重复选中不回调。
两端构建、边界、源码Biome及GUI3113/Web718全量测试通过；两端16组样式夹具一致，
真实键盘手动激活与Tab离组通过。图标夹具使用同尺寸占位，未验证完整原生工作区流程。
未提交，DetailsPanel留待T2单独验证。详见迁移计划第11节。


### 轨迹详情 Tabs 保守迁移（T2，2026-09-11）

DetailsPanel仅替换页签条，原状态、请求、单个ActiveTab渲染与滚动容器保留。
先锁住旧行为再迁移，特别保留点击默认页签所记录的明确选择。页签条relative修复原语
首次滚动测量混入外层偏移的问题。两端84组断点/主题对照一致，真实键盘、滚动位置和
宽度拖拽对照通过。31项定向、GUI3114/Web718全量测试通过；最后布局修正后再跑构建、
定向回归与浏览器对照，未重复全量。内容Tab为视觉夹具占位，未验收完整原生工作区。
未提交，详细证据见迁移计划第12节。

### 长 TSX 职责拆分：SFTP 路径导航（2026-09-11）

WorkspaceSftpPanel 从 1990 行降至 1536 行，路径导航移至同目录 SftpPathNavigator，
面板和导航通过 workspaceSftpPaths 共享路径计算。搬移函数及剩余面板 AST 对比一致，
真实 React DOM 测试覆盖面包屑、键盘选择与过期请求隔离；UI 类型、双端构建、边界和
源码 Biome 通过。未验收实际 SFTP 服务或浏览器像素效果，未暂存/提交。
最长十个 TSX 的盘点与后续候选见 [长 TSX 拆分记录](ui-tsx-split-audit.md)。

### 分享脱敏单选项（2026-09-11）

HistoryShareModal 与 SharedHistoryManagerModal 接入共享无样式 RadioGroup，保留各自
label/原生按钮外观、业务重复选择守卫和 pending 禁用。管理弹窗显式保留 Enter 激活。
修改前后消费者 DOM 回归通过，两端构建通过，64 组双端控件样式比对一致；验证标签点击、
方向键、Enter 单次回调和可访问名称。尚未覆盖完整原生分享请求流程；详见迁移计划 R1。

### 按截图反馈移除装饰投影（2026-09-11）

按用户指定范围，移除 SSH 列表/网格连接卡悬停投影、视图切换的内阴影和彩色投影/描边，
以及共享 ChoiceCard 的选中投影（定时任务、Hooks）和 SkillsImportView 三种结果提示的厚投影。
保留选中底色、状态边框及键盘焦点提示；同步更新 ChoiceCard 原有样式断言。
13 项相关测试、三处源码 Biome 和差异空白检查通过。未执行浏览器视觉对照，未暂存或提交本批改动。

### 会话搜索 Command（2026-09-11）

搜索列表接入无样式 cmdk Command，保留原共享 Dialog/Input、列表样式和后端排序。
保留 Home/End 编辑、修饰方向键单步导航及结果/重试按钮原生激活；请求与关闭策略不变。
原语增加 ARIA 关联；输入法组合按键策略保持原行为。10 项定向回归、两端构建、类型与边界检查通过，
24 组双端样式对照一致，真实浏览器重试 Enter 单次点击。完整原生业务流程未验收；
具体范围和依赖影响见迁移计划 C1。

### Header 建议与右键菜单：本轮保留（2026-09-11）

已核对 ProviderModal/View 的多行 Header 编辑契约，并用 Base UI 1.6.0 浏览器原型验证：
已有查询重新聚焦时，Combobox 没有原实现的立即首项高亮，第一次 ArrowDown 的目标不同；
公共 API 没有受控高亮入口。本轮保留，避免维护两套互相纠正的高亮状态。

ContextMenu 原型确认即使 finalFocus=false，打开时也会将编辑输入框焦点移到 Popup；
MenuPopup 没有可配置 initialFocus，ContextMenu 的焦点域也不同。文件树、后台日志、
Git/预览/编辑器/SFTP 的坐标、选区、关闭与动作上下文继续由原实现维护，不改用内部
store 或伪造 DropdownMenu。详情、排除的假设和后续迁移条件见迁移计划 H1/M1。

本轮最终验证：GUI 3118 项、Web 718 项全部通过，两端生产构建、共享类型、源码 Biome、
UI 边界与 diff 检查通过。共用 jsdom 环境补齐 ResizeObserver 生命周期桩以支持 Command，
涉及尺寸断言的测试仍使用专用可驱动 observer。未提交、未推送；H1/M1 是有依据保留，
不计为已完成组件替换。完整收尾审计见迁移计划第 17 节。

### 设置错误框补齐消费者（2026-09-11）

Skills/Stt 共用 SettingsNotice compact-error；MemoryPanel/OrganizerHistoryModal 共用
multiline-error。保留原对齐、子节点文字、换行、外边距及两组不同的边框透明度。
四页外壳归一后的编译结果与工作区基线一致，DOM 类集合契约、共享类型、两端构建通过。
本批无全页面浏览器验收。边界检查仍受同期 NotifyToast 直接导入 Base UI 的改动影响；
未动该 Toast 文件，也未暂存、提交或推送。详见组件漂移审查第四批记录。

### Toast 标准原语第一批：NotifyToast（2026-09-11）

新增 components/ui/toast.tsx，集中导出 Base UI Toast 原语和 manager；NotifyToast 保留
items/onDismiss API，将计时、关闭、播报、悬停与键盘焦点暂停交给原语。Provider 仍在
各使用面内隔离，维持旧消息先显示、不限制条数、默认 5 秒和原定位。原颜色、尺寸、
圆角、阴影、模糊保留；动画改为原语状态驱动 CSS，保留 20px 位移、200/160ms 和缓动。
关闭回调不因父级删除再次触发；更新同 ID 内容不会重复添加。手势关闭暂不启用。

5 项定向测试通过，覆盖真实 React DOM、StrictMode、顺序、内容更新、错误优先级、
描述关联、关闭、实例隔离、悬停和 F6 聚焦暂停/恢复。共享 UI 类型、双端生产构建、
源码 Biome 通过；原语类型也经 ui/toast 导出以满足导入边界。
Web 开发夹具中三种通知 × 深浅主题的几何、字体、间距、配色、圆角和阴影计算值一致；
截图人工复核一致，浏览器无错误。夹具已移除，未验收原生 WebView/屏幕阅读器或窄屏。
扫描和导入的页面专属提示留待下一批迁移，保留此前无阴影样式及各自关闭规则。
未暂存或提交本批改动。

### Toast 第二批：Skill 扫描与导入结果（2026-09-11）

SurfaceToast 以页面内单条结果为边界，复用 Base UI 的 Provider/Viewport/Root 和关闭管理，
提供 top-end/bottom-end 两个位置规格，业务页面保留卡片内容并使用 Title/Description/Close。
扫描保持 6500ms，每次扫描分配新的 toastId 重启生命周期；删除页面自建通知计时器，
扫描按钮本身的完成态计时器保留。导入三种结果 timeout 默认为 0，手动关闭，原警告优先、
其次错误、最后成功的展示顺序保留，失败明细仍可滚动。悬停/键盘焦点暂停由标准原语处理。

18 项相关测试通过；最后收拢位置 API 后复跑生命周期测试、类型检查和两端构建。
源码 Biome、UI 边界、差异空白检查通过。五种状态乘深浅主题共 10 组浏览器夹具中，
位置、大小、文字、背景、边框、阴影、圆角和间距一致；截图人工复核通过，无浏览器错误。
随后位置类原样收进共享组件，未改变值。临时夹具已移除。
未执行真实 Skill 导入、原生 WebView 或屏幕阅读器验收，未暂存或提交本批改动。

### Toast 第三批：根级挂载与函数调用（2026-09-11）

按用户确认改为全局通知范式：桌面 App 与 Gateway 的语言上下文中各挂 Toaster，
通过 body Portal 在窗口右上角统一堆叠。toast.success/error/warning 返回 ID，支持
同 ID 更新、duration、description、action、onDismiss 和 dismiss。根挂载前的调用
先缓冲，取消可移除缓冲项。通知仍使用 Base UI 管理暂停计时、键盘焦点和播报。

删除 NotifyToast、SurfaceToast 局部容器；聊天两端不再维护 notifyItems，图片反馈改为
函数适配，Skills 页删除扫描/导入通知状态和 JSX，以及批量撤销的独立计时器与浮条。
结果提示保留普通背景、细边框、无阴影；聊天通知保留原彩色皮肤。结果卡宽度统一为
24rem 并限制在窄屏以内；所有通知改为窗口右上角，这是本批明确的布局收敛。
扫描时长 6500ms、导入常驻、撤销 6000ms 保留。页面专属 ID 以 useId 区分，并在卸载
或进入新的批量选择操作时清理；撤销根据最新选中集合计算 FLIP，再恢复操作前快照。

GUI 3122 项、Web 718 项全量测试通过；UI/GUI/Web 类型、双端构建、源码 Biome 与
UI 边界检查通过。旧局部 DOM/浮条数量断言迁移到新调用契约；短时钟测试改用虚拟时钟，
避免并行负载下受真实调度时间影响。新增根挂载前通知、取消、StrictMode、同 ID 更新、
跨页面存续、关闭去重、暂停恢复和操作回调覆盖。真实浏览器确认通知位于模态框上方，
撤销可点击且执行一次，关闭按钮可访问；已保存截图并移除临时夹具（包括新增暂存记录）。
未验收真实 Skill 导入及系统屏幕阅读器。本轮未提交代码，保留用户已有暂存工作。
使用接口见 [全局 Toast](ui-toast.md)。

### Toast 位置保留（2026-09-11）

按用户后续要求撤回上段的统一右上角布局：Skill 扫描结果恢复右下角，批量撤销恢复
底部居中，其他通知保留右上角。根 Toaster 通过 position 分配独立通知区域，同 ID
更新省略 position 时保留原位置。统一函数入口和原时长保持不变。

本次 UI 类型检查、3 个源码文件的 Biome 检查及 13 项相关测试通过；新增 DOM 回归
覆盖挂载前分区通知、同 ID 更新保留位置、按 ID 关闭与全局关闭。未补做浏览器视觉验收。

### 设置页闪回修正（2026-09-11）

设置浮层重复打开时原先无条件回到 entering，可能短暂露出底层聊天页；现在仅从 closed
执行入场，已打开或正在关闭时重新打开直接保持 open。SettingsShell 的外部目标分区
原先通过 effect 同步，现改为在子页面提交前同步，避免提交旧分区后再切换。

新增真实 React DOM 回归覆盖重复打开、关闭中重开、迟到退场事件，以及分区跳转的提交
顺序。相关 5 项测试、共享 UI 类型检查及两处源码 Biome 检查通过；尚未在桌面端复现并
逐帧核验用户看到的闪回，因此仍需以实际使用表现确认是否存在其他触发路径。

### 卡片重复外框排查（2026-09-11）

MCP Store 与 Skills 商店卡片的 focus:ring 会在鼠标聚焦、预览关闭后回焦时叠加原有
border；改为仅 focus-visible 显示焦点环，保留键盘导航提示。语音输入供应商卡片的
选中态移除额外 ring，保留原边框、背景和阴影。排查共享 UI 及两端 TSX 中 focus:ring
和 primary ring 用法；输入框聚焦、键盘焦点环及单独承担边框的 ring 未统一删除。

3 处源码 Biome、共享 UI 类型检查及 10 项相关测试通过；未进行本轮浏览器视觉回归。

用户要求缩小已安装 Skill 卡片中 Switch 的右侧空隙：操作区向右扩展 6px，右侧
留白由 14px 调整为 8px，正文和底部分隔线仍使用原内边距。卡片操作区 Biome 及
10 项相关现有测试通过。复核 MCP/Skills 商店的鼠标焦点外框和 STT 选中态额外 ring
修复均仍在当前源码中。

### Skills 切回时加载布局跳动（2026-09-11）

录屏显示 MCP 切回 Skills 时先出现扫描/准备提示，再出现分类和卡片。SkillsHub 原先
每次从空数组挂载并强制扫描，useDeferredValue 的空 initialValue 又延迟一次列表。
现从既有 discovery 缓存初始化首帧，入页发现请求复用缓存和在途请求，主动扫描仍强制
刷新；安装、删除等原有缓存失效规则保留。首屏不再人为延迟已有列表，首次未缓存加载
保留分类栏位置，初始 loading 同步初始化避免短暂显示空结果。

90 项现有 Skills 测试和共享 UI 类型检查通过。新增缓存重入、强制刷新和删除失效测试；
原延迟列表源码断言更新为首屏保留、后续更新仍可延迟。尚未补录桌面端逐帧对比。

### Switch 圆点内侧间距纠正（2026-09-11）

用户明确指的是圆点距轨道右端的空隙，之前调整卡片操作区属于误解，现撤回 -mr-1.5。
ResourceActivationSwitch 小号轨道 36×20、圆点 14×14，开启偏移由 16.8 改为 19，
关闭偏移由 2.4 改为 3，使两端和上下留白均为 3（默认 16px 根字号）。使用 Tailwind
spacing 倍数随 rem 缩放，普通尺寸保持原样。源码 Biome 和 5 项卡片交互测试通过。

### 高级设置模型选择器去框（2026-09-11）

标题生成、Commit 备注生成和澄清模型共用的 CustomSettingsModelField trigger 移除
边框及阴影，使用透明底色，悬停/展开时轻微背景反馈，键盘 focus-visible 保留焦点环。
仅调整该字段外观，不修改菜单弹层和其他有意使用边框的选择器。

用户随后澄清模型选择器只需去掉阴影：恢复 CustomSettingsModelField 原有细边框及
明暗底色，仅将 shadow-sm 改为 shadow-none；保留键盘焦点提示。此前去框理解有误。
本次单处样式 Biome 检查通过。

### ModelPicker 样式职责整理（2026-09-11）

“使用当前模型”和普通模型共用 ModelOptionItem，集中行尺寸、选中背景、图标透明度
和勾选标记。trigger 的 default/compact/quiet/dashed 变体由 ModelPicker 维护，迁移
供应商新增入口、高级设置和记忆设置的覆盖类名；高级设置保留细边框及无阴影。移除
分组标题对 DropdownMenuItem 默认 flex、对齐、横向间距和 transition 的重复声明。
搜索过滤、分组折叠、弹层宽度、滚动区与 FocusGuard 隔离 wrapper 保留。

共享 UI 类型检查、源码 Biome、7 项相关测试通过；新增真实 DOM 测试验证搜索展开、
选择关闭、清除选择和 quiet 变体边框/阴影契约。未执行本轮浏览器视觉对比。


### DropdownMenu 默认样式收敛（2026-09-11）

按用户要求审查共享 UI、桌面及 Web 的 15 个 DropdownMenu 消费文件。14 个调用文件
移除非必要的字号、圆角、磨砂、投影、高亮及动画覆盖；桌面配置同步菜单只有必要的
多行布局，保留。另清理侧栏自定义动画常量内容及失效方向变量 CSS。菜单默认沿用
ui/dropdown-menu.tsx，保留宽高、滚动、截断、品牌、多行说明和危险操作语义；选中
背景归到 accent。模型菜单的触发器细边框不受影响。

Web 全量 718 项通过。桌面全量 3127 项中 3126 项通过，剩余一项要求 Git 分支菜单
保留已删除的动画；改为验证共享动画后，9 项相关回归通过。UI 类型、双端构建通过，
构建仍有既有大包提示。浏览器夹具确认模型行及分组均为 14px 字号、4px 圆角、32px
行高，选择后关闭且值更新，没有浏览器错误。夹具已删除，未逐个菜单做视觉验收。


## Chat 图片预览交互与组件收敛（2026-09-14）

按用户要求修正关闭行为、改善预览 UI，并收敛基础交互的维护责任：

- 全屏状态必须有真实弹窗元素，避免两个 `null` 相等导致普通预览误判全屏。
  浏览器退出原生全屏时关闭预览；显式退出全屏按钮保留预览。
- Esc、外部点击和嵌套菜单关闭交由 Base UI 的 Dialog/ContextMenu 管理。
  删除查看器的 Esc 捕获、菜单的 document 监听器、手写坐标碰撞及 ResizeObserver。
- 新增共享 `ui/context-menu.tsx`，复用现有菜单 Popup/Item 的样式和行为；共享菜单支持
 指定 Portal 容器，以适配原生全屏。默认挂载行为保持不变。
- Composer 附件、用户附件、工具图片、display_image 图集和查看器均使用原生 ContextMenu
  Trigger；删除各入口的菜单坐标/索引状态。方向键导航、菜单关闭和焦点恢复由库负责。
- 图片菜单与文件操作归入 `ImagePreviewMenu.tsx`；查看器保留图像测量、缩放、拖拽和旋转。
  文件系统可信附件校验、两端适配器、缓存及异步失败反馈保留。
- 预览工具按钮、关闭按钮和信息面板按钮复用共享 Button；按后续参考图改为沉浸式 lightbox，右上角显示更多/保存/关闭，底部为缩放胶囊。
  顺/逆时针使用独立图标，移除不合适的 application 角色。
- 图片快捷键不拦截菜单键盘操作和已选中文本的复制；重置快捷键不抢占 Ctrl/Command+0。

验证：共享 UI typecheck、GUI/WebUI 两端 build、修改 TSX 的 Biome 检查及图片查看器与
两端适配器测试通过。新增执行行为测试，验证保存同步打开选择器、取消不读取图片数据；
保留异步失败、可信附件、图片算法和大图片缓存测试，移除要求手写菜单监听器的源码断言。

临时浏览器页面加载真实组件，实测：实际 Composer 附件右键/点击打开、菜单方向键导航、
Esc 先关菜单再关预览、焦点回到入口、信息面板 Esc、遮罩关闭、全屏菜单旋转与 Esc、
窄屏菜单避让。干净浏览器会话无运行错误；临时页面已移除。未覆盖 Tauri 原生保存/剪贴板
的端到端操作。Shift+F10 的平台触发行为未通过本机浏览器验证，不据此声称已覆盖。

`pnpm check:ui-boundaries` 仍被未修改的
`crates/agent-ui/src/components/settings/SettingsCombobox.tsx` 直接导入 Base UI 阻挡；
该项不属于图片预览改动。构建有插件耗时与大分包提示，但均成功完成。


### 参考图驱动的沉浸式布局

用户提供截图后，将图片预览改为整窗暗色遮罩、居中图片、两侧翻页、右上角圆形操作按钮，
以及底部缩放胶囊。旋转、复制、信息和原生全屏保留在更多菜单/右键菜单；没有引入图片编辑功能。
缩放百分比反映相对图片原始尺寸的实际比例，点击百分比恢复适合窗口的视图。

共享 Dialog 新增显式 `lightbox` 布局，其他布局默认值不变。普通模式让空白区域的指针事件
落回 Base UI 的遮罩，继续由 Dialog 负责关闭；原生全屏时共享遮罩不在全屏层内，因此仅对
全屏 Popup 自身的空白点击补充关闭处理。图片、菜单、浮动按钮与信息面板接收正常指针事件。

验证包括宽/窄屏截图、留白关闭、更多菜单、全屏旋转与 Esc、左右键翻页、按钮缩放与拖拽。
自动化 CLI 的滚轮事件实际落在 (0,0)，因此额外以图片定点 WheelEvent 验证缩放逻辑；
不将这一项当作硬件滚轮端到端验证。图片测试和共享 Dialog 测试通过，临时页面已移除。

## 长对话历史预加载与原生滚动条（2026-09-14）

- 桌面历史预加载距离由固定 480px 改为至少 1600px / 两个视口；合并 scroll、scrollend 和向上 wheel 检查，成功提交后复查，不要求用户再次滚动。以历史首项标识防止同一边界重复请求，失败后允许下一次手势重试。
- 历史窗口解析借用 RawValue，仅展开返回窗口中的消息，保留消息引用、分页 offset 与渲染边界对齐；不再为一页深拷贝整个大分段。
- 桌面聊天保留原生 viewport，轨道左右留白、点击区域加宽，并通过底部轨道边距避开浮动输入区。虚拟列表启用动画帧尺寸测量，避免在 ResizeObserver 回调中同步写布局。
- 验证：桌面类型检查与构建通过；73 项既有滚动/行模型/宽度测试、3 项分页事件行为测试、6 项 Rust 历史窗口测试通过。原生 WKWebView 交互及帧率仍需在开发版实测，不以测试通过替代流畅度结论。网页端分页触发和样式本轮未修改。

### 快速滚动的预渲染缓冲

- 历史预取不等于虚拟列表预渲染。桌面转录从单向固定 480px 改为半屏双向像素缓冲（320–800px），叠加滚动方向 1.5 屏（960–2400px）；反向滚动前已有内容可显示，初次滚动也有双侧缓冲。
- virtual-core 新增可选 `overscanPx`，默认 0，其他列表保持既有行为。仍使用成本预算补充邻居，未扩大历史加载页数或停用虚拟化。
- 25 项虚拟内核测试通过，覆盖大跨度跳转、反向缓冲、挂载数量上限、锚定和滚动写入恢复；桌面与内核类型检查通过。Vite 已推送 dev 热更新；这些检查不能替代 WKWebView 实际帧率测量。

### 长对话页面重渲染隔离

- 修复 ChatPage 将上下文重建函数直接传入 useRef：改为惰性初始化，主题/侧栏等页面更新不再遍历全部消息。会话标识和子任务 store 管理器同步改为惰性创建。
- 回复悬停状态移入局部外部 store，只由操作栏订阅布尔状态；不再通过整个虚拟列表及 Markdown/工具行传递悬停 props。
- 真实 React DOM 回归测试验证连续 20 次父级重渲染只初始化一次，悬停只更新受影响操作栏；另 8 项悬停/分页/滚动回归和桌面类型检查通过。
- 完整活跃分段仍保留，供续聊、压缩与持久化使用；本轮未用展示窗口替代模型上下文。原生帧率改善仍需在 dev 长对话上复测。

### React 订阅与面板边界优化

- 按 `../pixreunion/.agents/skills/react-best-practices/SKILL.md` 的派生状态订阅、memo 边界和依赖范围规则检查桌面聊天页。
- ChatPage 不再为视图入口转换整份轨迹消息；只订阅“是否已有助手回复”的 boolean，历史已含回复或草稿状态下无需实时订阅。轨迹转换和完整实时订阅保留在实际显示的 ConversationTrajectorySurface 内。
- 主会话 binding 使用完整依赖的 useMemo，编辑重发错误处理器稳定引用；工作台复用同一份输入禁用状态，去掉重复对象包装。实际 Pane 内容通过 memo 边界比较完整 binding 和身份，Context 的包装对象更新不再强制穿透到绑定未变化的面板。后台 binding 仍按现有路径读取实时运行态，没有忽略回调或缓存可变 registry 读取。
- 模型选项只依赖 customProviders；不浮动当前选中模型的列表无需因主题或其他设置更新而重建。
- 25 项相关测试通过：100 次流式更新不增加页面渲染，20 次 Context 包装更新不重渲染绑定未变的实际 Pane Host；换会话、binding 更新、ref 转移、回复落库/清空/草稿、水合禁用输入等行为保留。Pane 测试替换了重型叶组件，不代表原生布局/帧率验收。
- 桌面类型检查、构建、修改源码 Biome 检查通过；构建仍有既存大 chunk 警告。本轮不修改模型上下文、历史分页或虚拟列表阈值，原生 WKWebView 帧率仍需 dev 实测。

### Git 图颜色具名（2026-09-15）

- 五个仅供 Git 分支图使用的 `--ui-color-<hex>` 改为 `--git-review-graph-lane-1` 至 `--git-review-graph-lane-5`，保留色值与循环顺序；现有 `ref-local`、`ref-remote`、`ref-base` 语义名不变。
- 更新共享 gitGraph 引用及测试颜色解析器的 lane 前缀支持，旧变量名无剩余引用。桌面／网页共用的 Git 图测试全部通过，仍以解析后的原始五个色值作断言；源码 Biome 与 diff 检查通过。未更改分支布局和绘制行为。

### Git 图配色归组件维护（2026-09-15）

按用户后续要求，将 lane 色板和亮暗 ref 配色从全局 tokens 移入 Git 历史组件旁的 `HistoryView.css`，由 `HistoryView.tsx` 引入，变量限定在 `.git-review-history-row` 内。行背景及悬停／选中／菜单打开时的图背景映射也从 base.css 一并迁入。保留语义变量名、全部原色值和绘图规则，不新增全局颜色别名。

Git 图测试改为从组件样式读取色板，仍验证原来的五个色值及顺序；19 项测试通过。两端类型检查和构建、修改文件 Biome 检查通过。未进行原生界面截图验收。

### 工作树入口与创建表单（2026-09-15）

- 参考 Lody 将工作树入口靠近分支选择的组织方式，在聊天输入区分支按钮旁增加“新工作树”，复用现有创建与侧边栏打开流程；现有分支菜单入口保留并改用共享 Button。
- 创建表单默认呈现起始分支、新分支名；目录名和父目录收进原生 details 高级选项。保留目录名随分支名生成、手动覆盖及默认位置规则，位置说明和错误反馈始终可见。
- 起始分支 Label 与 SelectTrigger 通过 id 关联，恢复默认位置图标补 aria-label，提交错误使用 alert。补齐中英文入口与高级选项文案。
- 本轮是创建入口前置，没有引入“发送时自动创建工作树”的新会话执行模式。验证采用两端类型检查/构建、现有分支确认测试及 Biome；尚未做原生视觉验收。

### 工作树 UI 层级继续整理（2026-09-15）

- 按用户明确范围只调整现有 UI：创建弹窗使用中性图标与更清楚的标题层级；仓库路径改成带图标的信息行，避免看起来像可编辑输入框。
- 基于分支与新分支名统一填充式字段字号和标签层级；高级选项以分隔线和可旋转箭头表达展开状态，去掉多层边框及说明底色。沿用原生 details 和原有目录选择行为。
- 分支菜单的新建分支与新建工作树操作统一使用共享 Button，显式左对齐并统一小号高度；保留原有事件、禁用条件及提交逻辑。
- 两端类型检查与构建、分支确认测试及修改源码 Biome 检查通过，构建仍提示现有大 chunk。浏览器预览因工具无法验证管理员安全策略而未能打开，本轮没有截图或原生视觉验收；临时预览文件已清理。没有新增执行模式或自动创建工作树流程。

### 右侧工具面板开合动画（2026-09-15）

- 桌面右侧工具面板内层固定宽度并锚定右边缘，用 200ms 位移和透明度过渡完成滑入、滑出；外层仍一次提交占位宽度，避免长对话在动画每帧重新排版。关闭期间沿用延迟卸载，打开时立即挂载内容。
- 过渡属性补入 Tailwind v4 位移工具使用的独立 translate 属性；保留拖拽与立即关闭时禁用动画，以及减少动态效果设置。网页窄屏继续使用原有全屏淡入布局。
- 7 项工作台边界回归测试通过，两端类型检查与构建通过，并确认生成 CSS 包含 translate 过渡属性；未完成原生动画视觉验收。

### 右侧工具面板改用 Motion（2026-09-15）

- 用户反馈 CSS 版本收起效果仍不自然，要求采用标准动画库。复用已有 motion 依赖、LazyMotion/domAnimation 和 UI_MOTION_TRANSITION.collapse，不增加依赖。
- 外层由 Motion 动画驱动开合进度，桌面占位宽度随同一进度收缩；内部内容保持固定宽度并裁剪，避免终端与文件树反复换行。AnimatePresence 管理退出后的卸载，删除宽度 hook 中的延迟卸载定时器及重复可见状态。
- 同步保留 inert/aria-hidden、减少动态效果、拖拽即时响应与切换页面立即关闭；窄屏网页使用同一进度淡出。测试契约同步改为 Motion presence 与占位宽度协调，不再要求一次性改变宽度。
- 两端类型检查与构建、7 项工作台边界回归和修改源码 Biome 通过，已确认两端产物包含进度驱动的占位宽度规则。此方案为改进收起体验而恢复占位宽度的逐帧变化；聊天区仍会有布局开销，尚未完成原生开合手感和长对话帧率验收。

### 两侧栏统一标准组件（2026-09-15）

- 按用户要求，将主导航外壳迁入基于 shadcn/ui Base UI 的 SidebarProvider、Sidebar、Header/Content/Footer；保留受控开关和现有导航、会话列表业务，桌面使用占位与 offcanvas 容器，窄屏使用现有 Base UI Sheet 处理遮罩、Esc 与焦点管理。适配原生窗口相对定位及现有主题变量，不引入额外快捷键或 cookie 状态。
- 桌面和网页共用 WorkspacePanels，由 shadcn/ui Resizable 包装 react-resizable-panels 4 管理右侧分栏、拖拽和键盘调宽。删除 useRightDockPanelWidth 手写拖拽以及上一轮右侧 Motion 外壳。移动端右侧也统一为 Sheet；保持已有工具与聊天功能。
- 桌面开合保留聊天和工具内容实例，隐藏工具停止前台终端渲染；宽度只在用户调整后保存，使用库回调的布局比例，避免读取过渡前 DOM 尺寸。开合过渡服从减少动态效果设置，拖拽时关闭过渡。Sheet 过渡补齐 Tailwind v4 的 translate 属性。
- 27 项回归测试通过，包含真实 Base UI Sheet 的 Esc、焦点恢复，以及真实 Resizable 的键盘宽度保存、开合草稿保留、移动断点单实例。两端类型检查与生产构建、修改源码 Biome 和 diff 检查通过，桌面构建仍有现有大 chunk 提示。原生动画手感及长对话帧率仍需 dev 实机验收；标准组件替换本身不代表消除了聊天重排开销。

### Sidebar 完整源码与业务接入纠正（2026-09-15）

- 上轮 Sidebar 只迁移了外壳，未完成官方状态和菜单组合接入。本轮从 shadcn 官方 `base-nova/sidebar` registry 安装完整源码，保留 Provider 受控/非受控状态、独立 openMobile、toggleSidebar、快捷键与 cookie 写入，以及 variant、collapsible、Trigger/Rail、Group/Menu/Sub/Action/Badge/Skeleton 等完整接口。
- 桌面 ChatPage 与网页 GatewayApp 在工作台根部提供 Provider，删除页面内重复的 sidebarOpen useState。标题栏开关、导航开关、选择会话后关闭和设置入口关闭都操作同一 context，移动端关闭不会覆盖桌面状态；删除旧 onCloseSidebar 透传接口。
- 主导航使用 SidebarMenu/Item/Button；工作区与置顶区使用 Group/Label/Content；会话和项目主按钮使用 MenuButton，行内操作使用 MenuAction。保留会话虚拟列表、工作区分页、拖拽排序及菜单业务控制，不将虚拟定位节点强行替换成静态菜单列表。
- 明确的项目适配：桌面容器相对工作台定位，沿用 272px 和现有移动断点、字体与颜色变量；标签本地化；关闭后 inert；减少动态效果；Cmd/Ctrl+B 在输入框、编辑器、终端中不拦截。官方 cookie 写入保留，但没有额外添加 cookie 读取恢复功能。
- 94 项相关回归通过，包含真实组件的受控 Trigger、快捷键与编辑保护、移动端独立状态、Esc/焦点恢复、菜单重命名/删除/移动、分区折叠、搜索定位与拖拽模型。视觉手感与长会话帧率尚未完成 dev 实机验收。

- 最终两端类型检查与生产构建通过；7 个修改源码的 Biome 和 diff 检查通过。最终 ref 契约调整后再次运行 5 项真实外壳测试通过。dev 服务仍在 1420 端口，未修改暂存区或创建提交。

### Sidebar 视觉回归修正（2026-09-15）

- 用户截图确认完整 Sidebar 源码的默认视觉与设置页不一致：Group/Header 的 padding 和 gap 与业务布局叠加，GroupLabel 固定高度扩大留白，sidebar-inner 背景覆盖 settings rail，MenuButton 的 active 背景又覆盖行级选中背景。
- 保留完整 Provider、Trigger、移动端独立状态、菜单组合与键盘行为，调整组件的默认外观以适配现有设置页：移除重复结构留白、继承宿主背景；菜单使用 foreground/10 选中和 foreground/5 悬停、紧凑行距与颜色过渡；会话/项目由整行统一画选中背景，内部按钮透明并移除多余纵向 padding。图标遵循调用处现有尺寸。
- 17 项真实外壳、菜单和工作区交互回归通过。本轮按用户截图和源码修正，尚未完成原生截图对照验收。

- 两端类型检查与构建、修改源码 Biome 和 diff 检查通过。

### Sidebar 开合性能回退修复（2026-09-15）

- 隔离真实 React 组件复现：200 个 memo 菜单行开合 5 次，普通 Button 没有额外行更新，原 SidebarMenuButton 因无条件订阅 context 产生 1,000 次行更新。将 context 消费移入专用 tooltip 子组件，普通菜单保持官方 render/state 接口，修复后同场景额外行更新为 0。
- Provider 通过提交后同步的 ref 读取最新受控值与回调，保持 setOpen/toggleSidebar 引用稳定，事件内立即更新值以支持同批次连续 functional toggle；避免 ChatPage 原生事件监听随每次开合重建。受控行为与移动端分离状态保留。
- WorkspacePanels 的 ResizeObserver 使用回调给出的 contentRect，宽度约束在变化停止 120ms 后合并提交，避免动画每帧读布局并传播 React context。初次挂载立即测量，卸载清理待执行计时器。此项仅减少约束更新，不声称移除了 CSS 宽度动画或虚拟列表测高的所有布局成本。
- 18 项真实组件回归通过，包括 200 行性能回归、稳定回调、同批次双切换和原有菜单/移动端交互。尚未进行 dev 与线上生产版本的同条件帧率对比。

- 最终两端类型检查与构建、修改源码 Biome、diff 检查通过。

### 按录屏调整固定开关与开合方式（2026-09-15）

- 对用户两段录屏提取时间序列帧，确认当前右侧开关随主栏边界移动，左侧原生开关也按开合状态切换坐标。左侧固定在红绿灯旁；ChatHeader 的右侧操作区固定于窗口右上方，工具面板标题栏留出对应空间。
- 保留 Sidebar/Resizable 状态和拖拽行为，移除左侧占位 width 与右侧 Panel flex-grow 过渡。聊天栏提交最终布局一次；左侧 offcanvas 和右侧独立内容层仅做 translate 过渡，关闭时右侧保留保存宽度以完成退出，打开与拖拽时内容使用当前面板宽度。减少动态效果与 inert 保留。
- 13 项外壳和工作台回归通过。录屏证明的是修改前问题；修改后的实机开合与帧率尚未录制对照，不将 DOM 测试当作流畅度验收。

- 两端类型检查与构建、修改源码 Biome 与 diff 检查通过。

### 修正开合期间正文跳变（2026-09-15）

- 新录屏显示上一轮一次提交聊天布局造成正文横向跳变，侧栏独立滑动无法掩盖该变化。恢复 Sidebar gap 的 width 连续过渡，与容器 translate 统一使用 200ms ease-out；右侧恢复 Resizable 两面板同节奏 flex-grow 过渡，移除额外的内容 translate 动画。
- 右侧内容保持目标宽度并裁剪，拖拽时随实时面板宽度变化。固定 trigger、普通 MenuButton 无额外 context 订阅、稳定回调和合并约束更新保留。连续布局仍有浏览器排版成本，不能据源码断言达到参考录屏流畅度。
- 尝试直接打开 dev 进行视觉验收，浏览器工具因管理员安全策略无法验证而拒绝访问 localhost；没有绕过该限制。

- 13 项回归、两端类型检查和构建、修改源码 Biome 与 diff 检查通过。

### 聊天加载骨架同步布局（2026-09-15）

- 按用户截图更新 PaneLoadingSkeleton 的 conversation 分支：删除多余的假标题栏，沿用正文 max-w-transcript-web 宽度变量、居中与 px-5/py-4 留白，替换整屏百分比布局。终端分支保留原有结构。
- 使用共享 Skeleton 呈现消息占位，显式 animate-none，保持静态加载策略与 status/aria-busy。原有正文切换覆盖层和 Suspense 共用该组件，输入框继续由真实宿主显示。

### 固定 header 操作区脱离分栏（2026-09-15）

- 用户反馈右侧开关无响应及 header 不齐。右侧操作区虽为 fixed，此前仍位于 Resizable 主栏和带拖拽标记的 header 内。本轮通过 React Portal 挂载到 document.body，显式 pointer-events-auto/no-drag，脱离分栏的裁剪与堆叠上下文；卸载时自动清理。
- MacOsTitleBarToggle 根据现有 AppKit 红绿灯坐标发布共同中心线；左右按钮、中间 AppWorkbenchChrome 与右侧面板标题使用该中心线确定位置/高度。网页版回退 24px 中心线，保持 48px header。
- 新增真实 ChatHeader + ProjectToolsPanelToggle 回归，验证操作区不在裁剪容器内、连续点击打开/关闭及节点身份、卸载清理。加上现有外壳与工作台共 14 项通过。DOM 环境无法证明原生窗口命中测试结果，尚需 dev 实机验收。

- 两端类型检查与构建、修改源码 Biome 和 diff 检查通过。

### 手写骨架统一共享组件（2026-09-15）

- 扫描桌面端、网关 Web 与共享 UI 的骨架、脉冲动画及 loading 分支静态占位。供应商用量、输入附件、消息附件、Markdown 图片、工作区目录、隧道列表和终端加载页均改用共享 Skeleton；保留尺寸、颜色和静态加载策略。
- Skeleton 增加 Base UI render 组合与 data-slot，行内图片使用 span，避免 Markdown 段落嵌套 div。目录列表移除父层 pulse，由占位组件统一处理减少动态效果偏好。
- 运行状态灯、扫描指示与下载进度保持原有语义。已有标准骨架不重复改造。
- GUI 与 Gateway Web 类型检查及构建通过；Skeleton 行内 DOM、ref/无障碍属性、静态动画覆盖及工作台加载回归共 3 项通过；修改源码 Biome 与 diff 检查通过。浏览器工具受管理策略限制，本轮未做视觉验收。

### 右侧分栏首次展开回归（2026-09-15）

- 将真实 Resizable 回归的初始状态改为关闭，补验首次打开与再次打开时实际 flex-grow 大于零。发现原来禁用的 separator 在展开后缺失面板关联，键盘调整报 Matching panels not found。
- 桌面端 separator 保持注册及启用，关闭时使用 invisible/w-0/pointer-events-none 隐藏并取消命中，避免 disabled 切换破坏关联。移动端保持 Group 禁用与 Sheet 展示。
- 6 项外壳回归通过，共享 UI 类型检查通过。这确认修复了首次关闭的分栏交互错误；尚未在原生窗口复现用户点击无响应，不能据此断言全部原因已解决。原生自动化连接超时，正在等待用户确认按钮是否变为选中状态。

### 统一顶栏改为外壳布局（2026-09-15）

- 按用户要求，将 GUI ChatPage 和 GatewayAppView 的 AppWorkbenchChrome 提升到分栏之外。外壳采用顶栏 + 可收缩内容区，左侧导航与 Resizable 工作区都位于下方独立定位容器。
- ChatHeader 的右侧按钮移回正常 Flex 行，移除 document.body Portal、fixed、z-49、纵向 translate 及 pr-24/pl-232px 补偿。左右开关持续占据顶栏两端，聊天/轨迹位于中间。
- macOS 控件作为 navigationActions 行内渲染，原生红绿灯安全预留及整行高度仍在桌面适配层集中处理；去除侧栏重复标题栏占位和右侧工具栏避让悬浮按钮的留白。
- 原生与 Web 构建通过。回归改为验证顶栏位于分栏之外、按钮 DOM 属于 header、真实右侧开关从初始关闭展开并分配宽度。dev 原生自动化尚未成功连接；线上窗口不作为本轮验收依据。

### 顶栏延续侧栏背景（2026-09-15）

- 按用户截图修正顶栏整条白色造成的侧栏背景断层。顶栏左侧装饰背景使用与侧栏一致的 bg-settings-rail、--sidebar-width 和 200ms ease-out 宽度过渡；关闭侧栏时收起，移动抽屉模式隐藏。
- 仅修改背景层，按钮仍在正常 Flex 布局中，不恢复 Portal 或固定坐标定位。

### Windows 统一顶栏兼容（2026-09-15）

- WindowsTitleBar 增加 controlsOnly 组合方式，沿用原有 Tauri 窗口操作与监听清理。ChatPage 将窗口按钮放入统一 ChatHeader 的正常布局，AppChrome 不再为聊天页叠加第二行标题栏。
- 设置覆盖层显示时仍由 AppChrome 提供独立标题栏；AppBootShell 和聊天错误边界回退也保留窗口控制。非 Windows 环境不会渲染这些控件。
- 中间 flex-1 空白区域显式标记 data-tauri-drag-region，窗口按钮区显式禁止拖拽。桌面隐藏侧栏内重复开关，移动抽屉保留关闭入口。
- 模拟 Win32 + Tauri 环境验证单行 header、最小化/关闭调用、最大化标签更新、侧栏开关、订阅清理、启动/错误回退和非 Windows 隐藏；新增 2 项通过，原有 16 项相关回归通过。两端类型检查和生产构建、Biome/diff 检查通过。
- 本机无法执行 Windows 原生验收。WebView2 命中测试、高 DPI、系统缩放、拖拽吸附与 Windows 11 Snap Layout 不属于上述 DOM 模拟结论。

### 搜索会话迁移 coss Command 结构（2026-09-16）

- 参考用户指定的 https://coss.com/ui/docs/components/command 及官方 command/autocomplete 源码，使用现有 Base UI Dialog + Autocomplete 独立实现项目 Command 封装，替换原 cmdk 转导出。沿用项目语义颜色，并采用搜索框、圆角结果面板、分组及底部键盘提示结构。
- ConversationSearchDialog 移除 activeIndex、手写高亮和方向键逻辑，保留后端全文搜索排序、防抖、过期请求防护、失败重试、草稿/持久化会话打开语义。关闭客户端过滤；保留 Home/End 编辑输入，IME 组合期间不执行列表快捷键。
- 使用真实 Dialog/Autocomplete 更新 DOM 回归与跨工作区导航回归。24 项相关测试通过，两端类型检查及生产构建通过，修改源码 Biome/diff 检查通过。弹窗高度受视口约束；尚未完成 dev 原生视觉验收。

### 搜索弹窗配色与设置页统一（2026-09-16）

- 根据用户三张对照截图，将 Command 顶底背景由偏蓝的通用 muted 改为 settings-tile；浅色选中态复用 settings-active，暗色使用 settings-tile-hover，保持结果行高亮可辨。
- 外框、面板分隔线与键盘提示边框改为低透明度 foreground，减少蓝灰描边。正文、辅助文字、弹窗结构与交互保持原有实现。
- 修改源码 Biome、共享 UI 类型检查与 diff 检查通过；未声称完成 dev 视觉验收。

### 对话/轨迹切换回归主区域（2026-09-16）

- 按用户要求，GUI 与 Gateway 的 ConversationViewTabs 从窗口级 AppWorkbenchChrome 移入 WorkspaceMainPanel 内的独立 header。左右侧栏展开/收起时，切换器始终跟随主区域布局，不依赖固定坐标或 Portal。
- 保留已有展示条件：聊天有回复且非多分屏时显示；多分屏继续使用 PaneChrome 的轨迹入口。窗口按钮与侧栏开关仍归统一顶栏。
- 7 项工作台边界回归通过，两端类型检查通过；未进行原生窗口视觉验收。

### 修正主区域 header 纵向错位（2026-09-16）

- 用户截图确认上一轮独立 h-11 header 多占一行。删除该行，统一顶栏内分为导航区、主区域 header、右侧操作区和 Windows 窗口控制区，保持同一中心线。
- 导航区在桌面展开时使用与侧栏相同的 --sidebar-width 预留宽度，并扣除外层左侧 padding；主区域 header 因而位于侧栏右侧。收起时回到导航控件所需宽度，移动抽屉模式不预留侧栏宽度。无需 Portal、负 margin 或纵向定位补偿。
- 10 项 header/Windows/工作台回归通过；两端构建用于检查 TS 与新增响应式宽度样式。此项替代上一轮单独新增主区域 header 行的实现。
